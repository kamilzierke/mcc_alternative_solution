substitutions:
  device_name: mcc-pro
  friendly_name: MCC Pro
  tc1047_adc_full_scale_v: "3.02"
  tc1047_temp_offset_c: "0.0"
  tc1047_scan_interval: "10s"
  # Calibration rule:
  # tc1047_adc_full_scale_v = V_multimeter_at_mux_Z_or_TC1047_VOUT * 1023 / raw_avg
  # Current value 3.02 comes from: ESPHome 0.8761V vs multimeter 0.801V, after prior 3.30V assumption.

esphome:
  name: ${device_name}
  friendly_name: ${friendly_name}
  comment: "MegaCell Charger Pro: sequential TC1047 temperature scan C1..C16 through 74HC4067, enabled by PCF8574 P0. Diagnostics retained."
  includes:
    - mcc_diag_helpers.h
  on_boot:
    priority: 600
    then:
      - lambda: |-
          mccdiag::init_pins();
          mccdiag::disable_tcas();
          mccdiag::setup_bq_web_handler();
          ESP_LOGW("mcc_boot", "MCC TC1047 all-slot diagnostic booted. Temp mux enable: PCF8574 P0 LOW. Mux channels Y0..Y15 map to C1..C16. ADC full-scale %.3fV.", ${tc1047_adc_full_scale_v}f);

esp8266:
  board: esp12e
  restore_from_flash: false

logger:
  level: INFO
  baud_rate: 115200

wifi:
  id: mcc_wifi
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  power_save_mode: none
  min_auth_mode: WPA2

captive_portal:

api:
  id: api_server
  reboot_timeout: 0s

ota:
  - platform: esphome

web_server:
  port: 80
  version: 3
  include_internal: true

# I2C observed on MCC Pro ESP8266 board.
# Expected boot scan: 0x27, 0x3C, 0x41, 0x4F, 0x70, 0x71.
# New trace: INA219 U47=0x41; U34=0x45 if visible on the selected bus/path.
i2c:
  sda: GPIO4
  scl: GPIO5
  scan: true
  frequency: 50000Hz
  timeout: 1s

globals:
  - id: oled_page
    type: int
    restore_value: false
    initial_value: "0"

script:
  - id: scan_all_tc1047_temperatures
    mode: single
    then:
      - lambda: |-
          static const char *TAG = "mcc_temp_scan";

          // Temperature mux enable line:
          // 74HC4067 E pin is active LOW.
          // E -> PCF8574 IC25 pin 4 = P0.
          // R4 = 10k pull-up to VCC, therefore default PCF 0xFF keeps mux disabled.
          uint8_t pcf_before = 0xFF;
          bool pcf_read_ok = mccdiag::i2c_read_u8(mccdiag::ADDR_PCF8574, pcf_before);
          if (!pcf_read_ok) {
            ESP_LOGE(TAG, "PCF8574 0x27 read failed; cannot enable TC1047 mux");
            return;
          }

          uint8_t pcf_enable = pcf_before & 0xFE;  // P0 LOW, preserve P1..P7.
          bool pcf_enable_ok = mccdiag::i2c_write_u8(mccdiag::ADDR_PCF8574, pcf_enable);
          if (!pcf_enable_ok) {
            ESP_LOGE(TAG, "PCF8574 write enable failed: before=0x%02X enable=0x%02X", pcf_before, pcf_enable);
            return;
          }

          delay(10);

          float temps[16];
          float volts[16];
          float raws[16];
          uint16_t mins[16];
          uint16_t maxs[16];

          constexpr uint8_t samples = 32;
          const float adc_full_scale = ${tc1047_adc_full_scale_v}f;
          const float temp_offset = ${tc1047_temp_offset_c}f;

          for (uint8_t ch = 0; ch < 16; ch++) {
            // Mapping confirmed for C8 and inferred linearly for the same mux:
            // C1-Y0, C2-Y1, ..., C8-Y7, ..., C16-Y15.
            mccdiag::mux_select(ch);
            delay(25);

            uint16_t min_raw = 1023;
            uint16_t max_raw = 0;
            uint32_t sum_raw = 0;

            for (uint8_t i = 0; i < samples; i++) {
              uint16_t raw = analogRead(A0);
              sum_raw += raw;
              if (raw < min_raw) min_raw = raw;
              if (raw > max_raw) max_raw = raw;
              delay(4);
              yield();
            }

            float raw_avg = (float) sum_raw / (float) samples;
            float voltage = raw_avg * (adc_full_scale / 1023.0f);
            float temp_c = ((voltage - 0.500f) * 100.0f) + temp_offset;

            temps[ch] = temp_c;
            volts[ch] = voltage;
            raws[ch] = raw_avg;
            mins[ch] = min_raw;
            maxs[ch] = max_raw;

            ESP_LOGI(TAG,
                     "C%02u/Y%02u TC1047: raw_avg=%.2f min=%u max=%u voltage=%.4fV temp=%.1fC",
                     ch + 1, ch, raw_avg, min_raw, max_raw, voltage, temp_c);
          }

          bool pcf_restore_ok = mccdiag::i2c_write_u8(mccdiag::ADDR_PCF8574, pcf_before);
          if (!pcf_restore_ok) {
            ESP_LOGE(TAG, "PCF8574 restore failed: attempted restore=0x%02X", pcf_before);
          }

          id(c1_tc1047_temperature).publish_state(temps[0]);
          id(c2_tc1047_temperature).publish_state(temps[1]);
          id(c3_tc1047_temperature).publish_state(temps[2]);
          id(c4_tc1047_temperature).publish_state(temps[3]);
          id(c5_tc1047_temperature).publish_state(temps[4]);
          id(c6_tc1047_temperature).publish_state(temps[5]);
          id(c7_tc1047_temperature).publish_state(temps[6]);
          id(c8_tc1047_temperature).publish_state(temps[7]);
          id(c9_tc1047_temperature).publish_state(temps[8]);
          id(c10_tc1047_temperature).publish_state(temps[9]);
          id(c11_tc1047_temperature).publish_state(temps[10]);
          id(c12_tc1047_temperature).publish_state(temps[11]);
          id(c13_tc1047_temperature).publish_state(temps[12]);
          id(c14_tc1047_temperature).publish_state(temps[13]);
          id(c15_tc1047_temperature).publish_state(temps[14]);
          id(c16_tc1047_temperature).publish_state(temps[15]);

          ESP_LOGW(TAG,
                   "TC1047 scan complete: PCF before=0x%02X enable=0x%02X restore=%s ADC_full_scale=%.3fV mapping C1-Y0..C16-Y15",
                   pcf_before, pcf_enable, pcf_restore_ok ? "OK" : "FAIL", adc_full_scale);

interval:
  - interval: ${tc1047_scan_interval}
    then:
      - script.execute: scan_all_tc1047_temperatures

  - interval: 5s
    then:
      - lambda: |-
          id(oled_page) = (id(oled_page) + 1) % 3;

sensor:
  - platform: wifi_signal
    id: wifi_signal_db
    name: "WiFi Signal dB"
    entity_category: diagnostic
    update_interval: 30s

  - platform: template
    id: c1_tc1047_temperature
    name: "C1 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c2_tc1047_temperature
    name: "C2 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c3_tc1047_temperature
    name: "C3 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c4_tc1047_temperature
    name: "C4 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c5_tc1047_temperature
    name: "C5 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c6_tc1047_temperature
    name: "C6 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c7_tc1047_temperature
    name: "C7 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c8_tc1047_temperature
    name: "C8 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c9_tc1047_temperature
    name: "C9 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c10_tc1047_temperature
    name: "C10 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c11_tc1047_temperature
    name: "C11 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c12_tc1047_temperature
    name: "C12 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c13_tc1047_temperature
    name: "C13 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c14_tc1047_temperature
    name: "C14 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c15_tc1047_temperature
    name: "C15 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c16_tc1047_temperature
    name: "C16 TC1047 Temperature"
    icon: "mdi:thermometer"
    device_class: temperature
    state_class: measurement
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: never

  - platform: template
    id: c16_bq_present
    name: "C16 BQ24195 Present"
    icon: "mdi:chip"
    entity_category: diagnostic
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: c16_bq_reg08
    name: "C16 BQ24195 REG08"
    icon: "mdi:counter"
    entity_category: diagnostic
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: c16_bq_reg09
    name: "C16 BQ24195 REG09"
    icon: "mdi:counter"
    entity_category: diagnostic
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: c16_bq_reg0a
    name: "C16 BQ24195 REG0A"
    icon: "mdi:counter"
    entity_category: diagnostic
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: c16_bq_dpm_stat
    name: "C16 BQ24195 DPM Status"
    icon: "mdi:current-dc"
    entity_category: diagnostic
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: c16_bq_pg_stat
    name: "C16 BQ24195 Power Good"
    icon: "mdi:power-plug"
    entity_category: diagnostic
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: c16_bq_vsys_stat
    name: "C16 BQ24195 VSYS Status"
    icon: "mdi:power-socket"
    entity_category: diagnostic
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: c16_ina_bus_voltage
    name: "C16 INA219 Bus Voltage"
    icon: "mdi:flash-triangle"
    entity_category: diagnostic
    device_class: voltage
    state_class: measurement
    unit_of_measurement: "V"
    accuracy_decimals: 3
    update_interval: never

  - platform: template
    id: c16_ina_shunt_voltage
    name: "C16 INA219 Shunt Voltage"
    icon: "mdi:sine-wave"
    entity_category: diagnostic
    device_class: voltage
    state_class: measurement
    unit_of_measurement: "mV"
    accuracy_decimals: 2
    update_interval: never

text_sensor:
  - platform: wifi_info
    ip_address:
      id: mcc_ip_address
      name: "IP Address"
      entity_category: diagnostic

  - platform: template
    id: c16_bq_charge_status
    name: "C16 BQ24195 Charge Status"
    icon: "mdi:battery-sync"
    entity_category: diagnostic

  - platform: template
    id: c16_bq_fault_status
    name: "C16 BQ24195 Fault Status"
    icon: "mdi:battery-alert"
    entity_category: diagnostic

font:
  - file:
      type: gfonts
      family: Roboto Mono
    id: oled_font
    size: 9
    bpp: 1
    glyphs:
      - " !#%()+,-./0123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz"

display:
  - platform: ssd1306_i2c
    model: "SSD1306 128x64"
    address: 0x3C
    update_interval: 1s
    lambda: |-
      auto temp_text = [](auto *sensor) -> const char * {
        static char buffers[16][8];
        static uint8_t index = 0;
        char *buffer = buffers[index++ % 16];
        if (sensor->has_state()) {
          snprintf(buffer, 8, "%4.1fC", sensor->state);
        } else {
          snprintf(buffer, 8, "--.-C");
        }
        return buffer;
      };

      it.printf(0, 0, id(oled_font), "MCC Pro");
      it.printf(106, 0, id(oled_font), "P%d/3", id(oled_page) + 1);
      it.line(0, 11, 127, 11);

      if (id(oled_page) == 0) {
        const bool wifi_ok = id(mcc_wifi).is_connected();
        const bool api_ok = id(api_server).is_connected();
        const char *ip = (id(mcc_ip_address).has_state() && !id(mcc_ip_address).state.empty())
                           ? id(mcc_ip_address).state.c_str()
                           : "no ip";

        it.printf(0, 15, id(oled_font), "WiFi: %s", wifi_ok ? "ok" : "down");
        it.printf(0, 25, id(oled_font), "IP: %s", ip);
        if (id(wifi_signal_db).has_state()) {
          it.printf(0, 35, id(oled_font), "RSSI: %.0f dBm", id(wifi_signal_db).state);
        } else {
          it.print(0, 35, id(oled_font), "RSSI: -- dBm");
        }
        it.printf(0, 45, id(oled_font), "API: %s", api_ok ? "ok" : "down");
        it.printf(0, 55, id(oled_font), "Scan: ${tc1047_scan_interval}");
      } else if (id(oled_page) == 1) {
        it.print(0, 15, id(oled_font), "Temps C01-C08");
        it.printf(0, 25, id(oled_font), "C01 %s C02 %s", temp_text(id(c1_tc1047_temperature)), temp_text(id(c2_tc1047_temperature)));
        it.printf(0, 35, id(oled_font), "C03 %s C04 %s", temp_text(id(c3_tc1047_temperature)), temp_text(id(c4_tc1047_temperature)));
        it.printf(0, 45, id(oled_font), "C05 %s C06 %s", temp_text(id(c5_tc1047_temperature)), temp_text(id(c6_tc1047_temperature)));
        it.printf(0, 55, id(oled_font), "C07 %s C08 %s", temp_text(id(c7_tc1047_temperature)), temp_text(id(c8_tc1047_temperature)));
      } else {
        it.print(0, 15, id(oled_font), "Temps C09-C16");
        it.printf(0, 25, id(oled_font), "C09 %s C10 %s", temp_text(id(c9_tc1047_temperature)), temp_text(id(c10_tc1047_temperature)));
        it.printf(0, 35, id(oled_font), "C11 %s C12 %s", temp_text(id(c11_tc1047_temperature)), temp_text(id(c12_tc1047_temperature)));
        it.printf(0, 45, id(oled_font), "C13 %s C14 %s", temp_text(id(c13_tc1047_temperature)), temp_text(id(c14_tc1047_temperature)));
        it.printf(0, 55, id(oled_font), "C15 %s C16 %s", temp_text(id(c15_tc1047_temperature)), temp_text(id(c16_tc1047_temperature)));
      }

button:
  - platform: restart
    name: "Restart"

  - platform: template
    name: "Read All TC1047 Temperatures Now"
    icon: "mdi:thermometer-lines"
    on_press:
      - script.execute: scan_all_tc1047_temperatures

  - platform: template
    name: "Read C16 BQ24195 Diagnostics"
    icon: "mdi:battery-search"
    on_press:
      - lambda: |-
          mccdiag::dump_bq_c16_u38();
